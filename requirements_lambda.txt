# Lambda deployment requirements
# These packages are installed in the Lambda package
# NOTE: numpy is NOT included here - it comes from the scikit-numpy:3 layer
# The layer is attached during Lambda creation in deploy_lambda.sh

boto3>=1.26.0
opensearch-py>=2.0.0
requests-aws4auth>=1.1.0
pytz>=2023.3
